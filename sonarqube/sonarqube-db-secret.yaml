apiVersion: v1
kind: Secret
metadata:
  name: sonarqube-db-secret
  namespace: jenkins
type: Opaque
stringData:
  # Plaintext for dev; use sealed-secrets or external secrets in production
  POSTGRES_DB: sonarqube
  POSTGRES_USER: sonar
  POSTGRES_PASSWORD: sonarpass
